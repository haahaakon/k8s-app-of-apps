apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: metallb-system
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "0"
spec:
  project: default
  source:
    repoURL: https://metallb.github.io/metallb
    chart: metallb
    targetRevision: 0.16.1
    helm:
      values: |
        controller:
          enabled: true
          labels:
            app: metallb
            component: controller
        # Using L2Advertisement mode only; frr-k8s (BGP) is unused and its
        # statuscleaner job runs hostNetwork/hostPort, violating baseline PSA.
        frrk8s:
          enabled: false
  destination:
    server: https://kubernetes.default.svc
    namespace: metallb-system
  syncPolicy:
    syncOptions:
      - CreateNamespace=true
    automated:
      prune: true
      selfHeal: true
